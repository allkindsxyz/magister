---
interface Props {
  class?: string;
  width?: number;
  height?: number;
}

const { class: className, width = 120, height = 55 } = Astro.props;
---

<img
  src="/media/magister-logo.png"
  alt=""
  class:list={['magister-logo', className]}
  width={width}
  height={height}
  decoding="async"
/>

<style>
  .magister-logo {
    display: block;
    width: auto;
    height: var(--nav-mark-height, 1.55rem);
    max-width: 8rem;
    object-fit: contain;
  }
</style>
